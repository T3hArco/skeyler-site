# this is the compass configuration file
# get compass! http://compass-style.org/install/
# install ruby, then `gem install compass`, then `gem install oily_png`
# then cd into the skeyler directory and `compass watch`. hurray!
# Require any additional compass plugins here.

http_path = "/"
css_dir = "media/css"
sass_dir = "media/scss"
images_dir = "media/images"
javascripts_dir = "media/js"

# You can select your preferred output style here (can be overridden via the command line):
# output_style = :expanded or :nested or :compact or :compressed

relative_assets = true

# To disable debugging comments that display the original location of your selectors. Uncomment:
# line_comments = false


# If you prefer the indented syntax, you might want to regenerate this
# project again passing --syntax sass, or you can uncomment this:
# preferred_syntax = :sass
# and then run:
# sass-convert -R --from scss --to sass scss scss && rm -rf sass && mv scss sass

require 'animation'